import React from 'react';
import FreeAgentTracker from './FreeAgentTracker';

const HockeyTracker = (props) => {
    return (
        <FreeAgentTracker sport="Hockey" />
    );
}

export default HockeyTracker;